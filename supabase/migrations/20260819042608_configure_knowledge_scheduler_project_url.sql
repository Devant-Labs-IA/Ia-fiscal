-- HISTORY MARKER ONLY.
-- A URL ambiental do projeto foi configurada na produção por RPC service-only.
-- O valor é específico do ambiente e nunca deve ser repetido por db reset/bootstrap.
-- O procedimento equivalente está em docs/runbooks/knowledge-phase2-release.md.
select 1;
